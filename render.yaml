services:
  - type: web
    name: etf-screener-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python etf_api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

